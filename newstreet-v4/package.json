{
  "name": "newstreet-v4",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run data && npm run images && astro build",
    "preview": "astro preview",
    "data": "node scripts/extract-projects.mjs && node scripts/extract-project-pages.mjs && node scripts/extract-press.mjs",
    "images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "xlsx": "^0.18.5"
  }
}
